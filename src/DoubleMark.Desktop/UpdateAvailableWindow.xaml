<Window x:Class="DoubleMark.Desktop.UpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:ui="clr-namespace:DoubleMark.Desktop"
        Title="Обновление DoubleMark"
        Height="420"
        Width="620"
        MinHeight="360"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource AppBackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>
    <Border Style="{StaticResource DialogWindowFrame}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Style="{StaticResource DialogTitleBar}" ui:DialogWindowChrome.IsDragArea="True">
                <TextBlock x:Name="TitleText" Text="Доступна новая версия" Style="{StaticResource DialogTitleText}"/>
            </Border>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="22,12,22,0">
                <StackPanel>
                    <TextBlock x:Name="VersionText" Style="{StaticResource SectionTitle}" TextWrapping="Wrap"/>
                    <TextBlock x:Name="ReleaseDateText" Style="{StaticResource MutedText}" Margin="0,8,0,12"/>
                    <TextBlock Text="Что нового" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>
                    <StackPanel x:Name="NotesPanel"/>
                    <TextBlock x:Name="StatusText" Style="{StaticResource MutedText}" TextWrapping="Wrap" Margin="0,16,0,0"/>
                    <ProgressBar x:Name="DownloadProgress" Height="8" Margin="0,12,0,0" Minimum="0" Maximum="100" Visibility="Collapsed"/>
                </StackPanel>
            </ScrollViewer>
            <UniformGrid Grid.Row="2" Columns="3" Margin="22,16,22,22">
                <Button x:Name="UpdateNowButton" Style="{StaticResource PrimaryButton}" Content="Обновить сейчас" Click="OnUpdateNowClick" Margin="0,0,6,0"/>
                <Button x:Name="LaterButton" Style="{StaticResource SecondaryButton}" Content="Позже" Click="OnLaterClick" Margin="6,0"/>
                <Button Style="{StaticResource SecondaryButton}" Content="Открыть сайт" Click="OnOpenSiteClick" Margin="6,0,0,0"/>
            </UniformGrid>
        </Grid>
    </Border>
</Window>
