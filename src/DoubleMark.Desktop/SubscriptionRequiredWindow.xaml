<Window x:Class="DoubleMark.Desktop.SubscriptionRequiredWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:ui="clr-namespace:DoubleMark.Desktop"
        Title="Нужна подписка DoubleMark"
        Height="300"
        Width="560"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{StaticResource AppBackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="0" GlassFrameThickness="0" CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>
    <Border Style="{StaticResource DialogWindowFrame}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Style="{StaticResource DialogTitleBar}" ui:DialogWindowChrome.IsDragArea="True">
                <TextBlock Text="Подписка DoubleMark" Style="{StaticResource DialogTitleText}"/>
            </Border>
            <StackPanel Grid.Row="1" Margin="22">
                <TextBlock Text="Для использования этой функции нужна активная подписка DoubleMark."
                           Style="{StaticResource SectionTitle}" TextWrapping="Wrap"/>
                <TextBlock x:Name="FeatureText" Text="Функция недоступна" Style="{StaticResource MutedText}" Margin="0,10,0,22"/>
                <UniformGrid Columns="3">
                    <Button Style="{StaticResource PrimaryButton}" Content="Выбрать тариф" Click="OnPricingClick" Margin="0,0,6,0"/>
                    <Button Style="{StaticResource SecondaryButton}" Content="Войти в другой аккаунт" Click="OnOtherAccountClick" Margin="6,0"/>
                    <Button Style="{StaticResource SecondaryButton}" Content="Закрыть" Click="OnCloseClick" Margin="6,0,0,0"/>
                </UniformGrid>
            </StackPanel>
        </Grid>
    </Border>
</Window>
