<UserControl x:Class="DubliMark.Desktop.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,0,24,24">
            <Border Style="{StaticResource HeroCard}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="История" Style="{StaticResource SectionTitle}"/>
                        <TextBlock Text="Поиск, фильтры, статусы, повторная печать и экспорт последних сканов."
                                   Style="{StaticResource MutedText}" Margin="0,6,0,0"/>
                    </StackPanel>
                    <TextBox Grid.Column="1" Style="{StaticResource InputBox}" Text="Поиск по GTIN, serial, GS..."/>
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Время" Style="{StaticResource MetricLabel}"/>
                        <TextBlock Grid.Column="1" Text="Статус" Style="{StaticResource MetricLabel}"/>
                        <TextBlock Grid.Column="2" Text="AI 91" Style="{StaticResource MetricLabel}"/>
                        <TextBlock Grid.Column="3" Text="AI 92" Style="{StaticResource MetricLabel}"/>
                        <TextBlock Grid.Column="4" Text="Действия" Style="{StaticResource MetricLabel}"/>
                    </Grid>
                    <Border Style="{StaticResource SoftSurface}" Margin="0,0,0,8">
                        <TextBlock Text="История текущего запуска отображается на Dashboard. Здесь подготовлена полноценная страница под расширенную историю, фильтры и сортировку."
                                   Style="{StaticResource MutedText}" TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
