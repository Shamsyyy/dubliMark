namespace DoubleMark.Core.Crpt;

/// <summary>
/// CRPT integration contour: sandbox vs production base URLs (see spec §6.1).
/// </summary>
public enum CrptEnvironment
{
    Sandbox,
    Production,
}
